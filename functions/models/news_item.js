"use strict";

export class NewsItem {
    constructor({ headline, subheadline, content }) {
        this.headline = headline;
        this.subheadline = subheadline;
        this.content = content;
    }
}